# API密钥配置
# ===========================================
# SiliconFlow / OpenAI 兼容接口 (provider: openai / siliconflow)
# 用于 text.api 或 vl.api 配置为 "openai" 或 "siliconflow" 时
OPENAI_API_KEY=your_api_key_here

# Anthropic 接口 (provider: anthropic)
# 用于 text.api 或 vl.api 配置为 "anthropic" 时
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# ===========================================

# 代码会自动根据 config.yaml 中的 provider 选择对应的环境变量
